PROGRAM example;

function aMinB(a, b): integer;
var
    c : integer;
begin
    c = b + a
end; 


function aPlusB(a, b): integer;
begin
    result = b + a
end; {end of myfunction}

VAR
    a : integer;
    b : integer;
    c : integer;
BEGIN
    bool = (a < b)
    IF (a < 0) THEN
        a = (10 + a) / 2
        a + 2
    ELSE
        (10 + 20) / 2
        a = 100
        IF (b > 2) THEN
            (2 / 2) < 6
        ELSE
            c = 2
END.
{EOF}